version: '3.9'
services:

  miniserver:
    build:
      context: ./05
    ports:
      - "80:81"


  nginx:
    image: nginx
    user: root
#    ports:
#      - "8080:80"
    depends_on: 
      - miniserver
#    links:
#      - miniserver:2.0
    volumes:
      - ./proxy_server/nginx.conf:/etc/nginx/nginx.conf
#      - /var/lib/nginx/body


